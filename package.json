{
  "name": "igaming",
  "version": "1.0.0",
  "packageManager": "pnpm@10.10.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm --filter igaming-shared build && dotenv -- turbo run dev --parallel",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "dotenv-cli": "^10.0.0",
    "turbo": "^2.5.8"
  }
}
